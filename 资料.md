# Python 项目启动器 V1 —— 开发需求文档

## 1. 项目目标

开发一个运行在 **Windows** 本机上的 Web 项目管理器，用于统一管理本机大量 Python 项目。

核心目标：

> 不需要打开终端、寻找项目目录、回忆启动命令，只需要打开网页，点击“启动”即可运行项目；同时可以从网页停止、重启、查看日志、编辑和删除项目。

这是一个**本地项目进程管理器**，不是远程服务器管理系统。

------

# 2. V1 核心功能

V1 必须实现以下功能：

### 项目管理

- 查看所有项目
- 添加项目
- 编辑项目
- 删除项目
- 保存项目配置
- 项目配置持久化

### 进程管理

- 启动项目
- 停止项目
- 重启项目
- 显示运行状态
- 显示 PID
- 显示运行时间
- 显示 CPU 使用率
- 显示内存使用量

### 日志

- 查看项目运行日志
- 实时显示 stdout
- 实时显示 stderr
- 清晰区分日志内容
- 项目停止后仍然可以查看本次运行日志

### 配置

每个项目可以独立配置：

- 项目名称
- 工作目录
- 启动命令
- 端口
- 环境变量
- 是否开机自动启动

------

# 3. 使用场景

例如用户有：

```
D:\Python\Project\
│
├── pc_tts
│   └── server.py
│
├── claude_nlp_pm
│   └── app_prod.py
│
├── stock_replay
│   └── app.py
│
└── cf_tools
    └── main.py
```

以前需要：

```
cd D:\Python\Project\pc_tts
uv run python server.py
```

现在打开：

```
http://127.0.0.1:xxxx
```

看到：

```
PC TTS
🟢 运行中

[停止] [重启] [日志] [编辑]
```

或者：

```
Claude NLP PM
⚪ 已停止

[启动] [编辑] [删除]
```

点击即可。

------

# 4. 技术栈

## 后端

使用：

```
Python
Flask
psutil
```

推荐：

```
Flask
Flask-CORS
psutil
```

如果需要实时日志：

```
Flask-SocketIO
```

但如果 Agent 认为 WebSocket 会增加复杂度，可以 V1 先使用 HTTP 轮询实现。

------

## 前端

V1 不要求复杂前端框架。

推荐：

```
HTML
CSS
JavaScript
```

如果使用框架，可以使用：

```
Vue 3
```

但不要为了使用框架而增加项目复杂度。

核心要求是：

> 页面漂亮、简单、响应迅速。

------

## 数据存储

V1 推荐：

```
SQLite
```

不要使用 MySQL。

项目配置数据量非常小，SQLite 足够。

数据库：

```
data/projects.db
```

------

# 5. 推荐项目结构

建议：

```
python-project-manager/
│
├── app.py
│
├── config.py
│
├── requirements.txt
│
├── manager/
│   ├── __init__.py
│   ├── process_manager.py
│   ├── project_manager.py
│   └── logger.py
│
├── models/
│   └── project.py
│
├── routes/
│   ├── projects.py
│   └── processes.py
│
├── templates/
│   └── index.html
│
├── static/
│   ├── css/
│   │   └── style.css
│   │
│   └── js/
│       └── app.js
│
├── logs/
│
└── data/
    └── projects.db
```

Agent 可以根据实际实现调整结构。

不要为了所谓“架构完整”拆分大量无意义文件。

------

# 6. 项目数据结构

每个项目至少包含：

```
id
name
working_dir
command
port
environment
auto_start
created_at
updated_at
```

例如：

```
{
    "id": 1,
    "name": "PC TTS",
    "working_dir": "D:\\Python\\Project\\pc_tts",
    "command": "uv run python server.py",
    "port": 8774,
    "environment": {
        "MODEL_PATH": "D:\\Models\\tts"
    },
    "auto_start": false
}
```

------

# 7. 启动命令设计

这是本项目非常重要的功能。

**不要假设所有项目都是 `python xxx.py`。**

启动命令必须允许用户自由填写。

例如：

```
python app.py
```

或者：

```
uv run python server.py
```

或者：

```
.venv\Scripts\python.exe app.py
```

或者：

```
python -m myapp
```

甚至允许：

```
npm run dev
```

只要 Windows 可以执行即可。

------

# 8. 工作目录

启动项目时必须使用项目配置中的：

```
working_dir
```

作为当前工作目录。

例如：

```
working_dir:
D:\Python\Project\pc_tts
```

执行：

```
uv run python server.py
```

等价于用户手动：

```
cd D:\Python\Project\pc_tts
uv run python server.py
```

这一点非常重要。

------

# 9. Windows 进程管理

后端使用：

```
subprocess.Popen()
```

启动项目。

必须记录：

```
PID
```

以及对应的：

```
project_id
```

例如：

```
project_id = 1
PID = 18324
```

内存中维护：

```
running_processes
```

类似：

```
{
    1: process_object,
    2: process_object
}
```

但**不要只依赖内存状态**。

因为：

- Flask 重启
- 管理器异常退出
- Python 进程被外部杀死

都有可能导致内存状态和真实进程状态不一致。

因此查询状态时，应通过 `psutil` 检查 PID 是否真实存在。

------

# 10. 启动逻辑

点击：

```
启动
```

后：

```
检查项目是否已经运行
        ↓
如果运行 → 返回“已经运行”
        ↓
如果没有运行
        ↓
检查 working_dir 是否存在
        ↓
检查启动命令
        ↓
创建日志
        ↓
subprocess.Popen()
        ↓
记录 PID
        ↓
返回成功
```

------

# 11. 停止逻辑

**不能简单只执行：**

```
process.kill()
```

因为 Python 项目可能产生子进程。

例如：

```
项目
└── python.exe
    ├── ffmpeg.exe
    ├── worker.exe
    └── subprocess
```

用户点击停止后，应该尽量停止整个进程树。

推荐使用：

```
psutil
```

流程：

```
找到项目 PID
     ↓
获取 Process
     ↓
获取 children(recursive=True)
     ↓
先终止子进程
     ↓
终止主进程
     ↓
等待退出
     ↓
仍未退出 → kill
```

最终确保项目相关进程尽可能全部退出。

------

# 12. 重启逻辑

点击：

```
重启
```

执行：

```
停止
 ↓
等待进程完全退出
 ↓
等待约 0.5~1 秒
 ↓
重新启动
```

不能出现：

```
旧进程还没退出
        ↓
新进程已经启动
```

否则可能产生：

```
端口被占用
```

------

# 13. 运行状态

项目至少有以下状态：

```
STOPPED
STARTING
RUNNING
STOPPING
ERROR
```

前端显示为：

```
🟢 运行中
🟡 启动中
🔵 停止中
⚪ 已停止
🔴 异常
```

------

# 14. 判断项目是否运行

不能只判断：

```
process.poll() is None
```

应该结合：

```
PID
+
psutil
```

判断。

如果进程已经被用户从任务管理器杀掉：

```
任务管理器
    ↓
python.exe 被杀
    ↓
项目管理器再次刷新
    ↓
应该显示“已停止”
```

而不是一直显示：

```
🟢 运行中
```

------

# 15. CPU / 内存

运行中的项目显示：

```
CPU: 2.3%
RAM: 82 MB
```

使用：

```
psutil.Process(pid)
```

获取：

```
cpu_percent()
memory_info()
```

注意：

CPU 第一次调用 `cpu_percent()` 可能没有有效值，因此需要合理处理第一次采样。

------

# 16. 运行时间

记录：

```
started_at
```

前端显示：

```
运行时间：02:31:42
```

格式：

```
<1小时：

23分15秒

>=1小时：

02:31:42
```

------

# 17. 日志系统

每个项目拥有独立日志。

例如：

```
logs/
├── project_1/
│   ├── 2026-09-01_13-20-10.log
│   └── 2026-09-01_14-32-21.log
│
└── project_2/
    └── 2026-09-01_13-30-12.log
```

------

# 18. stdout / stderr

启动进程：

```
stdout=subprocess.PIPE
stderr=subprocess.STDOUT
```

或者分别处理。

要求：

> 项目的终端输出可以在网页中看到。

例如项目输出：

```
Loading model...
Loading voice...
Server started.
Running at http://127.0.0.1:8774
```

网页实时显示：

```
┌──────────────────────────────────────────┐
│ PC TTS 日志                               │
├──────────────────────────────────────────┤
│ Loading model...                         │
│ Loading voice...                         │
│ Server started.                          │
│ Running at http://127.0.0.1:8774        │
└──────────────────────────────────────────┘
```

------

# 19. Windows 中文编码

必须考虑 Windows Python 项目的编码问题。

建议：

```
encoding="utf-8"
errors="replace"
```

如果某些程序输出 GBK 或其他编码导致乱码，要避免整个日志线程崩溃。

核心要求：

> 某个项目输出乱码不能导致项目管理器本身崩溃。

------

# 20. 日志实时显示

优先方案：

```
WebSocket
```

项目：

```
Python
 ↓
stdout
 ↓
日志线程
 ↓
WebSocket
 ↓
浏览器
```

如果不使用 WebSocket：

```
浏览器每 500~1000ms
 ↓
GET /api/projects/{id}/logs
```

V1 可以接受轮询。

------

# 21. 前端首页

首页是整个程序最重要的页面。

设计风格：

> 简洁、现代、偏开发者工具风格。

不要做得像后台管理系统一样复杂。

------

## 顶部

显示：

```
Python Project Manager

[ + 添加项目 ]
```

右侧可以显示：

```
运行中：3 / 8
```

------

# 22. 项目卡片

每个项目使用 Card。

例如：

```
┌────────────────────────────────────────────┐
│ 🟢  PC TTS                                 │
│                                            │
│ D:\Python\Project\pc_tts                   │
│                                            │
│ PID      18324                             │
│ CPU      2.3%                              │
│ RAM      82 MB                             │
│ 运行     01:32:18                          │
│                                            │
│ [停止] [重启] [日志] [编辑]                │
└────────────────────────────────────────────┘
```

停止状态：

```
┌────────────────────────────────────────────┐
│ ⚪ Claude NLP PM                           │
│                                            │
│ D:\Python\Project\claude_nlp_pm            │
│                                            │
│ 已停止                                     │
│                                            │
│ [启动] [编辑] [删除]                       │
└────────────────────────────────────────────┘
```

------

# 23. 添加项目

点击：

```
+ 添加项目
```

打开 Modal。

字段：

```
项目名称

工作目录

启动命令

端口

环境变量

开机自动启动
```

例如：

```
项目名称：
[ PC TTS                         ]

工作目录：
[ D:\Python\Project\pc_tts       ]

启动命令：
[ uv run python server.py        ]

端口：
[ 8774                           ]

环境变量：
[ + 添加环境变量 ]

☐ 开机自动启动

        [取消] [保存]
```

------

# 24. 编辑项目

点击：

```
编辑
```

显示和添加项目相同的表单。

保存以后：

```
更新数据库
```

如果项目正在运行：

> 修改配置后不要自动重启。

提示：

```
配置已保存，当前运行中的进程不会自动更新。
如需应用新配置，请手动重启项目。
```

------

# 25. 删除项目

点击：

```
删除
```

必须弹出确认：

```
确定删除项目“PC TTS”？

如果项目正在运行，建议先停止项目。

[取消] [确认删除]
```

建议规则：

**如果正在运行，不允许直接删除。**

提示：

```
请先停止项目，再删除。
```

这样可以避免留下孤儿进程。

------

# 26. 日志窗口

点击：

```
日志
```

打开大 Modal 或独立页面。

例如：

```
┌─────────────────────────────────────────────┐
│ PC TTS - 日志                       [×]     │
├─────────────────────────────────────────────┤
│                                             │
│ [13:20:01] Loading model...                │
│ [13:20:02] Loading voice...                │
│ [13:20:04] Server started                 │
│ [13:20:04] Port: 8774                     │
│                                             │
│                                             │
│                                             │
├─────────────────────────────────────────────┤
│ [清空当前日志]                  [自动滚动 ✓] │
└─────────────────────────────────────────────┘
```

日志区域：

```
黑色终端风格
等宽字体
自动滚动
```

------

# 27. API 设计

推荐 API：

### 获取项目

```
GET /api/projects
```

返回：

```
[
    {
        "id": 1,
        "name": "PC TTS",
        "status": "RUNNING",
        "pid": 18324,
        "cpu": 2.3,
        "memory": 82,
        "uptime": 5238
    }
]
```

------

### 获取项目详情

```
GET /api/projects/{id}
```

------

### 创建项目

```
POST /api/projects
```

------

### 修改项目

```
PUT /api/projects/{id}
```

------

### 删除项目

```
DELETE /api/projects/{id}
```

------

### 启动

```
POST /api/projects/{id}/start
```

------

### 停止

```
POST /api/projects/{id}/stop
```

------

### 重启

```
POST /api/projects/{id}/restart
```

------

### 获取日志

```
GET /api/projects/{id}/logs
```

------

### 获取状态

```
GET /api/projects/{id}/status
```

------

# 28. API 返回格式

统一：

成功：

```
{
    "success": true,
    "message": "项目启动成功",
    "data": {}
}
```

失败：

```
{
    "success": false,
    "message": "项目已经在运行"
}
```

HTTP 状态码也要合理使用。

例如：

```
200 成功
400 参数错误
404 项目不存在
409 状态冲突
500 服务端错误
```

------

# 29. 自动启动

项目配置：

```
auto_start
```

如果：

```
true
```

管理器启动以后：

```
Flask 启动
 ↓
读取所有项目
 ↓
寻找 auto_start=true
 ↓
逐个启动
```

注意：

不要同时瞬间启动几十个项目。

可以：

```
每个项目间隔 0.5~1 秒
```

或者后台异步启动。

------

# 30. 管理器自身启动

项目最终应该可以：

```
python app.py
```

启动。

或者：

```
uv run python app.py
```

启动。

启动以后：

```
http://127.0.0.1:5000
```

访问 Web UI。

------

# 31. 安全要求

V1 默认：

```
127.0.0.1
```

监听。

不要默认：

```
0.0.0.0
```

因为这个程序具有：

> 执行任意 Windows 命令

的能力。

因此默认只允许本机访问。

如果以后需要局域网访问，再增加：

```
用户名
密码
Token
```

等认证功能。

------

# 32. 命令执行安全

这是本项目最大的安全风险。

因为用户可以配置：

```
command
```

理论上相当于：

```
执行任意 Windows 命令
```

但这是一个**用户自己部署在自己电脑上的本地工具**，因此 V1 不需要做复杂沙箱。

但是必须：

- 默认只监听 `127.0.0.1`
- 不允许无意间暴露到公网
- 不允许前端直接执行任意命令 API
- 所有命令执行必须对应一个数据库中的项目

------

# 33. 错误处理

必须处理：

### 工作目录不存在

```
D:\Python\Project\abc

目录不存在
```

显示：

```
❌ 启动失败

工作目录不存在：
D:\Python\Project\abc
```

------

### 启动命令错误

例如：

```
xxx_command abc
```

显示：

```
❌ 启动失败

无法启动项目，请检查启动命令。
```

并且错误写入日志。

------

### 端口被占用

项目本身可能输出：

```
Address already in use
```

管理器至少要把该错误显示在日志中。

V1 不要求管理器主动检测所有端口冲突。

------

### 进程异常退出

例如：

```
python.exe
   ↓
运行
   ↓
异常退出
```

页面应该从：

```
🟢 运行中
```

变成：

```
🔴 异常退出
```

并且允许：

```
[重新启动]
[日志]
```

------

# 34. 并发要求

V1 不需要支持大量项目。

目标：

```
5~30 个 Python 项目
```

同时运行。

要求：

- 一个项目启动不能阻塞 Flask
- 一个项目日志读取不能阻塞其他项目
- 一个项目崩溃不能影响管理器
- 一个项目日志线程异常不能导致整个 Flask 崩溃

------

# 35. 重要：不要阻塞 Flask

错误示例：

```
process = subprocess.Popen(...)

process.wait()
```

不能在 Flask request 中直接等待。

因为：

```
项目运行 10 小时
```

那么 HTTP 请求就会一直卡住。

正确方式：

```
Flask
 ↓
启动 subprocess
 ↓
立即返回
 ↓
后台线程负责读取日志和监控进程
```

------

# 36. 日志线程

每个项目运行后可以有：

```
Process
Log Reader Thread
Monitor Thread
```

例如：

```
项目
│
├── subprocess
│
├── stdout reader
│
└── process monitor
```

项目停止后：

```
释放线程
关闭文件
清理内存状态
```

避免线程泄漏。

------

# 37. 项目状态刷新

前端每：

```
1~2 秒
```

刷新一次状态即可。

例如：

```
setInterval(loadProjects, 2000)
```

不要求 V1 使用复杂实时状态系统。

日志则可以单独使用：

```
WebSocket
```

或者：

```
500ms~1s polling
```

------

# 38. UI 交互要求

所有操作都应该有明确反馈。

例如启动：

```
点击启动
 ↓
按钮变成“启动中...”
 ↓
禁止重复点击
 ↓
启动成功
 ↓
按钮变成“停止”
```

停止：

```
停止中...
```

重启：

```
重启中...
```

错误：

```
Toast / Alert
```

例如：

```
✓ 项目启动成功
```

或者：

```
✕ 项目启动失败
```

------

# 39. 不要重复启动

如果项目已经运行：

```
POST /api/projects/1/start
```

不能再启动一个。

返回：

```
{
    "success": false,
    "message": "项目已经在运行"
}
```

避免：

```
python app.py
python app.py
python app.py
```

导致多个实例。

------

# 40. 项目管理器自身重启

如果项目管理器 Flask 自己重启：

```
正在运行的项目
```

**V1 尽量不要因为管理器重启而杀掉它们。**

项目进程应该独立于 Flask 生命周期。

也就是说：

```
Python Project Manager
       │
       ├── Project A
       ├── Project B
       └── Project C
```

管理器重启：

```
Manager 重启
      ↓
A/B/C 尽量继续运行
```

管理器再次启动时：

```
通过保存的 PID
+
psutil
```

重新发现这些进程。

------

# 41. PID 持久化注意事项

不能只保存 PID 就认为：

```
PID 1234 = 我的项目
```

因为 Windows 进程退出以后 PID 可能被其他程序复用。

因此恢复状态时必须验证：

```
PID
+
进程名称
+
启动时间
```

最好保存：

```
pid
process_create_time
```

然后使用：

```
psutil.Process(pid).create_time()
```

进行验证。

如果不匹配：

```
认为原项目已经停止
```

不要误杀其他程序。

------

# 42. 项目启动信息

建议保存：

```
pid
started_at
process_create_time
```

数据库可以增加：

```
last_pid
last_start_time
```

------

# 43. 日志保留

V1 不需要复杂日志管理。

建议：

```
每次启动创建一个日志文件
```

例如：

```
logs/project_1/
    2026-09-01_13-30-22.log
    2026-09-01_15-12-05.log
```

暂时不自动删除。

以后 V2 再增加：

```
日志最大大小
日志保留天数
日志数量限制
```

------

# 44. 不需要 V1 实现的功能

以下功能全部放到 V2：

```
❌ 用户登录
❌ 权限系统
❌ 局域网访问
❌ Docker
❌ Linux 支持
❌ 远程服务器管理
❌ Git 自动部署
❌ Git pull
❌ 在线编辑代码
❌ Web Terminal
❌ CPU/内存历史图表
❌ 自动更新
❌ 插件系统
❌ Docker 管理
❌ Supervisor/PM2 集成
```

V1 的目标只有：

> **把本机 Python 项目管理好。**

------

# 45. 推荐页面布局

整体：

```
┌──────────────────────────────────────────────────────────────┐
│ Python Project Manager                       [+ 添加项目]    │
│                                                              │
│ 运行中 3/8                                                   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│ ┌────────────────────┐ ┌────────────────────┐               │
│ │ 🟢 PC TTS          │ │ ⚪ Claude NLP       │               │
│ │                    │ │                    │               │
│ │ PID: 18324         │ │ 已停止             │               │
│ │ CPU: 2.3%          │ │                    │               │
│ │ RAM: 82 MB         │ │                    │               │
│ │                    │ │                    │               │
│ │ [停止] [重启]      │ │ [启动]             │               │
│ │ [日志] [编辑]      │ │ [编辑] [删除]      │               │
│ └────────────────────┘ └────────────────────┘               │
│                                                              │
│ ┌────────────────────┐ ┌────────────────────┐               │
│ │ 🟢 Stock Replay    │ │ 🔴 CF Tools        │               │
│ │ ...                │ │ 异常退出           │               │
│ └────────────────────┘ └────────────────────┘               │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

桌面端优先。

------

# 46. 响应式

虽然主要运行在 PC，但至少支持：

```
1920x1080
1366x768
```

以及基本手机宽度。

桌面：

```
3列 / 2列
```

小屏：

```
1列
```

------

# 47. 项目排序

默认按照：

```
created_at
```

或者：

```
用户添加顺序
```

V1 不需要复杂拖拽排序。

------

# 48. 代码质量要求

要求：

- Python 类型提示尽可能使用
- 清晰的异常处理
- 不要吞掉异常
- 日志记录关键错误
- 不要使用全局变量保存所有核心状态
- 不要把所有代码写进 `app.py`

但也不要过度工程化。

------

# 49. requirements.txt

至少包含：

```
Flask
psutil
```

如果采用：

```
Flask-SocketIO
```

再加入：

```
Flask-SocketIO
```

如果使用 SQLAlchemy：

```
Flask-SQLAlchemy
```

------

# 50. 启动方式

项目最终应该做到：

```
cd python-project-manager

python app.py
```

然后：

```
http://127.0.0.1:5000
```

即可访问。

------

# 51. 第一阶段开发顺序

Agent 不要一次把所有功能一起写。

按照下面顺序实现。

### Phase 1

完成：

```
Flask
SQLite
项目 CRUD
```

能够：

```
添加
编辑
删除
查询
```

------

### Phase 2

加入：

```
subprocess
```

实现：

```
启动
停止
重启
```

------

### Phase 3

加入：

```
psutil
```

实现：

```
PID
CPU
内存
运行时间
状态检测
```

------

### Phase 4

加入：

```
stdout / stderr
```

实现：

```
日志
```

------

### Phase 5

完成前端 UI：

```
Dashboard
项目 Card
添加 Modal
编辑 Modal
日志 Modal
Toast
状态显示
```

------

### Phase 6

实现：

```
auto_start
```

------

### Phase 7

全面测试：

```
启动
停止
重启
异常退出
手动杀进程
工作目录不存在
启动命令错误
端口占用
管理器重启
多个项目同时运行
中文日志
```

------

# 52. V1 验收标准

最终必须满足：

### 项目管理

-  可以添加项目
-  可以编辑项目
-  可以删除项目
-  刷新网页后项目仍存在

### 启动

-  点击启动可以启动项目
-  使用正确的 working directory
-  使用用户填写的 command
-  不会重复启动

### 停止

-  可以停止项目
-  可以停止子进程
-  停止后状态正确

### 重启

-  可以重启
-  旧进程完全退出后才启动新进程

### 状态

-  显示 PID
-  显示 CPU
-  显示内存
-  显示运行时间
-  外部杀掉进程后网页状态能够恢复

### 日志

-  可以查看日志
-  可以看到 stdout
-  可以看到 stderr
-  日志不会阻塞 Flask
-  中文日志不会导致程序崩溃

### 自动启动

-  支持设置 auto_start
-  管理器启动后自动启动指定项目

### 安全

-  默认监听 `127.0.0.1`
-  不默认暴露到局域网/公网

------

# 53. Agent 开发原则

开发过程中遵守以下原则：

> **先做出能用的 V1，再考虑扩展。**

不要主动加入：

```
Docker
Redis
Celery
Kubernetes
微服务
复杂权限
复杂前端框架
```

这个项目的核心其实就是：

```
Web UI
   ↓
Flask API
   ↓
subprocess + psutil
   ↓
Windows Python 项目
```

保持简单。

------

# 54. 最终产品定位

最终这个软件不是：

> “企业级服务器监控系统”

而是：

> **一个个人开发者的 Windows 本地 Python 项目启动器。**

用户打开网页后，可以一眼看到：

```
我的所有项目
       ↓
哪些正在运行
       ↓
哪些停止了
       ↓
点击启动
       ↓
查看日志
       ↓
出问题点击重启
```

核心体验必须做到：

> **“我再也不用为了启动一个 Python 项目去找 CMD、找目录、找启动命令。”**

------

## 给 Agent 的最终执行指令

**请直接按照以上需求开发 V1。**

开发要求：

1. 先建立完整项目结构。
2. 先实现后端 CRUD。
3. 再实现进程启动/停止/重启。
4. 再实现 psutil 状态监控。
5. 再实现日志。
6. 最后完成前端 UI。
7. 每完成一个阶段都自行测试。
8. 不要为了扩展性引入不必要的复杂组件。
9. 所有路径必须兼容 Windows。
10. 最终保证执行：

```
python app.py
```

即可运行。

1. 完成后提供：

- 完整项目代码
- requirements.txt
- 数据库初始化方式
- 启动方式
- 使用说明
- 测试结果
- 已知问题

**不要只给代码片段，要生成一个可以直接运行的完整 V1 项目。**