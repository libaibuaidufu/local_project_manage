# 技术决策记录

需求文档在若干处留了选择空间（"如果 Agent 认为…可以…"）。这里记录每一处实际选了什么、为什么。

---

## 1. 实时日志：HTTP 轮询，不用 WebSocket

需求 20 允许两种方案，选了轮询。

**理由**：Flask-SocketIO 在 Windows 上需要 eventlet 或 gevent，两者都要 monkey-patch 标准库。
本项目的核心恰好是 `subprocess` + 线程 + 文件 IO —— 全部是被 monkey-patch 影响的部分。
为了一个"日志晚 800ms 显示"的体验差异，去赌 monkey-patch 后 subprocess 行为不变，不划算。

轮询的实现也不粗糙：请求带 `stdout_at` / `stderr_at` 字节偏移，服务端只返回新增内容，
没有新内容时响应体是空的。日志窗口关闭即停止轮询。

---

## 2. 子进程输出：直接重定向到文件，不用 PIPE ★

**这是整个项目最关键的一个决策。**

需求 18 写的是 `stdout=subprocess.PIPE`。没有采用，改成把文件句柄直接交给子进程。

**理由**：管道有固定大小的缓冲区（Windows 上通常 64KB）。用 PIPE 就必须有一个线程持续读它，
一旦这个读取线程死了、卡住了，或者管理器整个崩了，缓冲区填满后**子进程本身会被阻塞在
write 上** —— 项目就这么卡死了。这与需求 34「一个项目日志线程异常不能导致整个 Flask 崩溃」
和需求 40「管理器重启不应影响项目」直接冲突。

重定向到文件后：

- 子进程写文件从不阻塞，管理器死活与它无关
- 每个项目省掉一个读取线程，不存在线程泄漏（需求 36）
- 管理器重启后，子进程仍持有同一个文件句柄继续追加，**日志不断档**

**代价**：没法给每一行加管理器侧的时间戳（需求 26 的示意图里有 `[13:20:01]` 前缀）。
折中办法是在日志文件开头写一段头部，记录启动时间、工作目录、命令、端口、环境变量；
进程退出时追加尾部，记录退出时间、退出码、结束原因。排查问题需要的信息都在，
只是不精确到每行。稳定性优先于装饰性，这个交换值得。

---

## 3. stdout 与 stderr 分成两个文件

需求 18 说"或者分别处理"，选了分开。

**理由**：需求列表里明确要求"清晰区分日志内容"。合并成一个文件后无法再区分来源，
前端也就没法做 stdout / stderr 筛选。分开存成 `{run_id}.out.log` 和 `{run_id}.err.log`，
前端三个 tab（全部 / stdout / stderr）都靠同一份数据本地过滤，切 tab 不需要重新请求。

**注意**：stderr 在界面上用琥珀色而不是红色。很多 Python 框架（Flask 自己就是）把正常的
启动信息写到 stderr，全标红会让人以为出了错。

---

## 4. 日志解码：逐行解码，不是整块解码 ★

实现过程中被测试抓出来的一个真实缺陷，值得单独记一条。

最初 `decode()` 是整块处理：先试 UTF-8，失败再试 GBK，最后 `errors="replace"` 兜底。
`test_gbk_output_does_not_crash` 直接失败了。

**原因**：同一个日志文件里存在两种编码 —— 管理器写的头部是 UTF-8，项目自己输出的可能是 GBK
（Windows 上没设 `PYTHONIOENCODING` 时 `print` 中文就是 GBK，`.bat` 输出也是）。
整块解码时，无论选哪种编码都会在另一部分上失败，最后整个文件被 `replace` 成一片乱码。

**改法**：按 `\n` 切开，逐行独立解码。UTF-8 的头部行正确解码，GBK 的输出行也正确解码。
需求 19 的底线是"乱码不能导致管理器崩溃"，实际做到了"混合编码也能正确显示"。

顺带只归一化 `\r\n`，不动单独的 `\r` —— 进度条靠 `\r` 回到行首覆盖重写，
把它也换成 `\n` 会让一个进度条炸成几百行。

---

## 5. 数据库：标准库 sqlite3，不用 SQLAlchemy

需求 49 提到可以用 Flask-SQLAlchemy。没用。

**理由**：一张表、九个字段、六条查询。ORM 在这个规模上只增加一层抽象和一个依赖，
换不来任何东西。每次操作开一个短连接（`contextmanager` 管提交回滚），
顺带绕开了 sqlite3 连接对象不能跨线程用的限制 —— 而本项目确实有三类线程会写库
（请求线程、监控线程、自动启动线程）。

开了 WAL 模式，让监控线程写状态时前端仍能正常读列表。

---

## 6. 前端：原生 JS，不用 Vue

需求 21 说"不要为了使用框架而增加项目复杂度"，照做。

一个列表页加三个弹窗，原生 JS 完全够用，还省掉构建步骤 —— 改完刷新就生效。

**渲染优化**：列表 2 秒刷一次，如果每次重建整个 `innerHTML`，正在 hover 的按钮会闪、
焦点会丢。实现里给每张卡片算一个内容指纹，只有指纹变了才动那张卡片的 DOM。

**XSS**：项目名、路径、命令都是用户输入且会插进 `innerHTML`，统一走 `esc()` 转义。

---

## 7. 命令执行：`shell=True`

**理由**：需求 7 明确要求支持 `npm run dev`。Windows 上 `npm` 实际是 `npm.cmd`，
而 `CreateProcess` 不能直接执行 `.cmd` / `.bat`，必须经过 `cmd.exe`。
要支持"只要 Windows 能执行就行"，`shell=True` 是唯一的选择。

**代价**：记录的 PID 是 cmd.exe，真正干活的 python.exe 是它的子进程。
实测还更深一层 —— venv 里的 python.exe 是个 shim，会再 exec 一次真正的解释器：

```
cmd.exe (记录的 PID)
├── conhost.exe
└── python.exe (venv shim)
    └── python.exe (真正的解释器)
```

所以 CPU / 内存必须按**整棵进程树**汇总，停止时必须**递归杀整棵树**。
只看或只杀直接子进程，前者永远显示 0%，后者会留下两个孤儿进程。这两点都实现了并验证过。

**安全性**：`shell=True` 通常是注入风险，但这里命令本身就是用户配置的、要执行的东西，
不存在"可信命令被注入不可信参数"的情形。真正的防线是需求 32 要求的：默认只监听回环地址、
每条命令都必须对应数据库里的一个项目、不提供"执行任意命令"的裸接口。这三条都做到了。

---

## 8. 监控：一个全局线程，不是每项目一个

需求 36 的示意图是每个项目配一个 monitor thread。改成所有项目共用一个。

**理由**：30 个项目就是 30 个线程，而它们做的事完全相同 —— 每 1.5 秒采样一次。
一个线程遍历所有项目即可，线程泄漏的可能性直接归零。

线程内部每个项目的采样都包在独立的 try/except 里，单个项目出问题不影响其他项目，
整个循环体再包一层，保证监控线程绝不退出（需求 34）。

**psutil 的坑**：`cpu_percent(None)` 返回的是"距上次调用以来的平均值"，
所以必须复用同一个 `psutil.Process` 对象 —— 每次 `new` 一个只会永远得到 `0.0`。
实现里为每个 runtime 维护 `pid -> Process` 缓存，新出现的子进程先预热一次。

**端口监听检测**：`psutil.net_connections()` 在 Windows 上不便宜，绝不能放进每次状态查询。
改成监控线程每个周期算一次全局 LISTEN 端口集合，状态查询只做集合查找。

---

## 9. 停止：CTRL_BREAK → terminate → kill 三段式

需求 11 只要求"terminate 不行就 kill"，实际多加了前置一步。

**理由**：`terminate()` 在 Windows 上是 `TerminateProcess`，进程没有任何清理机会 ——
`finally` 不跑、`atexit` 不跑、临时文件不删、数据库连接不关。
先发 `CTRL_BREAK_EVENT` 给程序一个体面退出的机会，2.5 秒不退再强来。

这也是启动时加 `CREATE_NEW_PROCESS_GROUP` 的原因：只有子进程自己是进程组组长，
才能对它单独发信号，而不会把 CTRL_BREAK 也发给管理器自己。

三段总耗时最坏 7 秒（2.5 + 2.5 + 2）。实测正常项目在第一步就退出，约 0.1 秒。

**同步执行**：停止在请求线程里同步完成，没有做成异步任务。7 秒的最坏情况可以接受，
换来的是"接口返回时进程一定已经死了"这个强保证 —— 重启逻辑直接依赖它，
不需要额外的状态机去等旧进程退出（需求 12 要求旧进程完全退出后才能启动新的）。

---

## 10. PID 复用防护：同时校验创建时间 ★

需求 41 提到了这一点，实现时当成硬性约束处理，因为**认错进程的后果是杀掉用户的其他程序**。

保存 `last_pid` 的同时保存 `last_create_time`。任何一处要"认领"或"终止"一个 PID 之前，
都必须先比对创建时间（容差 1 秒）：

- `_adopt_previous()` —— 接管前校验，不匹配就标记为已停止
- `_kill_tree()` —— 终止前校验，不匹配直接返回，什么都不做
- `_is_alive()` —— 存活检查里也校验，创建时间变了就认为原进程已死

三处都有专门的测试守着，其中两个测试拿测试进程自己当靶子：
故意用错的创建时间去调 `_kill_tree`，然后断言测试进程还活着。

---

## 11. 管理器退出时不杀子进程

需求 40 要求"尽量不要因为管理器重启而杀掉项目"，实现上做成了完全不杀。

`shutdown()` 只停监控线程。子进程因为 `CREATE_NEW_PROCESS_GROUP` 独立于管理器的进程组，
管理器被 terminate、被 Ctrl+C、甚至崩溃，项目都继续跑。

重新启动后 `_adopt_previous()` 通过 `PID + 创建时间` 重新接管，运行时间从原始的
`create_time` 算起（而不是从接管时刻重新计时），日志也接着往同一个文件写。

实测验证过：kill 掉管理器 → 项目三个进程全部存活 → 重启管理器 → 状态 `RUNNING`、
PID 不变、`adopted: true`、运行时间连续 → 点停止 → 整棵树 0.1 秒内干净退出。

---

## 12. 状态语义：非 0 退出码 = ERROR

**理由**：需求 33 要求进程异常退出后显示"🔴 异常退出"并提供重启按钮。

副作用：从任务管理器强杀进程时退出码非 0，所以会显示 ERROR 而不是 STOPPED。
严格说这不算"异常"，但从用户视角两者等价 —— 都是"没在跑了，可以点启动"。
要区分"用户杀的"和"程序自己崩的"，在 Windows 上没有可靠手段，不值得为此增加复杂度。

用户主动点"停止"时会设 `intentional` 标记，这种情况一定记为 STOPPED，不会误报异常。

被接管的进程没有 `Popen` 句柄，读不到退出码，一律记 STOPPED —— 宁可漏报异常，
不要因为"拿不到信息"就误报异常。

---

## 13. 关闭 Flask 重载器

**理由**：Werkzeug 的重载器会 fork 出第二个进程，两个进程都会执行 `bootstrap()` ——
`auto_start` 的项目会被启动两遍，监控线程也会翻倍。需求 39 明确不允许重复启动。

`use_reloader=False`，`debug` 默认关闭（需要时 `PPM_DEBUG=1`）。

---

## 14. 强制控制台 UTF-8

实测发现的一个真实崩溃，值得记录。

启动横幅里有中文。Windows 控制台默认用系统代码页，中文系统是 cp936 没问题，
但**英文系统是 cp1252，放不下中文，`print` 会直接抛 `UnicodeEncodeError` 把管理器崩掉。**

实测确认（`PYTHONIOENCODING=cp1252` 下复现了 traceback），修复方式是在 `main()` 里
任何输出之前 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`。
修复后在 cp1252 下横幅正常显示中文。

---

## 15. 工作目录不存在时允许保存，启动时才拦

**理由**：用户可能先登记项目、稍后再创建目录。保存时硬拦会很烦。

折中：保存成功，但在返回消息里附一句"注意：工作目录当前不存在，启动时会失败"，
前端用琥珀色 toast 显示。真正启动时才硬性拦截，返回 400 并把状态标成 ERROR（需求 33）。

---

## 16. 「清空当前日志」只清视图，不删文件

需求 26 的界面里有这个按钮。实现成只清空浏览器里的显示。

**理由**：进程正持有那个文件的句柄，并且是以追加模式打开的。从外部截断文件后，
子进程下一次写入仍从原来的偏移开始，中间会留下一大段 NUL 字节，日志反而更难看。
文件留在 `logs/` 里也没什么坏处，需要回看历史时正好用得上（日志窗口有运行记录下拉框）。

按钮点击后的 toast 会明确说明"日志文件保留在 logs/ 目录"，不会让用户误解。

---

## 17. 日志读取的两个保护

**单次上限 256KB**：首次打开一个跑了一整天的项目，日志可能有几十兆。
超过上限时只取尾部，并在开头标注"（日志过长，仅显示最近部分）"。

**只返回完整行**：文件末尾不完整的那一行留到下次读取。否则可能把一个多字节 UTF-8 字符
劈成两半，两次请求各拿到一半，前端显示乱码。

**路径穿越防护**：`run_id` 会拼进文件路径，用正则严格限定格式
（`\d{4}-\d{2}-\d{2}_\d{2}-\d{2}-\d{2}(-\d{3})?`），不匹配直接返回 400。
有专门的测试用 `../../../etc/passwd` 等输入验证。

---

## 18. 启动时自动备份数据库 ★

V1 开发过程中发生过一次真实事故：手动测试时用 `rm -rf data logs` 清空测试现场，
把用户已经录入的真实项目配置一并删掉了（当时库里已有用户数据，而测试脚本想当然地
认为那是测试数据）。

**新增行为**：`init_db()` 在打开任何连接之前，把 `projects.db` 复制为 `projects.db.bak`，
只保留一代。

**为什么这么做**：
- 备份时机选在"连接全部关闭后"——本项目是每操作短连接，此刻 WAL 已合并回主文件，
  主库静止，直接 `copyfile` 就是安全的，不需要 SQLite 的 backup API。
- 成本约 12KB 一次文件复制，可以忽略。
- 它防不住 `rm -rf data`（整个目录连备份一起删），真正防住那种事故的是：
  **手动测试一律用 `PPM_DB_PATH` 指向临时目录，永远不碰真实 `data/`**。这是
  `config.py` 里留这个环境变量的本意。备份是第二道网，防的是单文件损坏和误删。

**教训记录在这里的原因**：这类事故不会在代码评审里暴露，只会在复盘里暴露。

---

## 19. 明确没做的事

需求 44 列的 V2 功能一律没做。另外这些也没加，因为需求 53 要求"保持简单"：

- 没有 Docker、Redis、Celery
- 没有 waitress / gunicorn，用 Flask 开发服务器（`threaded=True`），本机自用足够
- 没有 CPU / 内存历史图表
- 没有主动的端口冲突检测（需求 33 明确说 V1 不要求，端口占用错误会出现在日志里）
- 没有日志自动清理（需求 43 说 V2 再做）
- 没有认证（需求 31 说 V1 不需要，靠只监听回环地址防护）

---

## 20. 备份走「网页下载 + 导入」，不是自动云备份 ★

背景：#18 记录的那次数据丢失事故之后，用户提出"网页上增加一个下载数据"。

**形式**：顶栏「下载数据」按钮导出 JSON 备份文件；「导入数据」按钮选文件恢复。
导出按钮是纯 `<a href="/api/projects/export" download>`，不经过任何 JS——
下载这条路径上少一层会出错的东西，JS 挂了按钮也照常工作。

**导入语义选择 —— 合并，不替换**：

- 按名称匹配：文件里的项目，库里同名 → 更新配置；没有 → 新增。
- 库里有、文件里没有的项目：**完全不动**。导入永远只做加法和更新。
  替换语义的导入是另一种形式的"导错一个文件把现有数据全冲掉"——
  刚因为数据丢失加了这个功能，不能再引入一个新的丢失途径。
- 匹配键用名称而不是 id：数据丢失后重建库，id 会重新排，名称才是
  跨备份稳定的人类可读标识。代价：改过名的项目会被当成新项目导入，
  出现重复——可接受，肉眼可辨。
- 单条数据非法只跳过该条，原因写进返回消息，不拖垮整批。校验走的
  是和手工添加完全相同的 `validate()`，不存在"导入路径绕过校验"这回事。

**备份内容选择 —— 只存配置，不存运行时状态**：PID、启动时间、退出码
属于当时那台机器的瞬时信息，恢复时没有意义，还会误导（恢复出一个
"RUNNING"状态但进程并不存在）。文件带 `format` 版本号和 `exported_at`，
将来格式变了可以做迁移；也接受裸数组，方便手工编辑过的文件。

**导入也提示运行中项目**：更新的项目里有正在运行的，返回消息会附一句
"新配置需手动重启后生效"——和编辑项目的提示（需求 24）同一条规则。
